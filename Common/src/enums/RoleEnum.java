package enums;
/**
 * Enum that defines the role of a user in the Messages sent to and from the server and client and in the DB
 */
public enum RoleEnum {
    CEO, STORE_MANAGER, CLIENT, CUSTOMER_SERVICE_WORKER, STORE_WORKER, COMPANY_MARKETING_WORKER, SERVICE_EXPERT, DELIVERY_MAN
}